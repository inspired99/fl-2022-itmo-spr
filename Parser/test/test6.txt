my_fun1() {
    print(x) # комментарий
}

main() {}

my_fun2(x, y) {
    fn(main);
    my_fun3(a);
    a = fn(fn(x), fn, my_fun2, my_fun2());
    x();
    if (x) return(x + y - "Hello")
    else return(y+1);
    my_fun3();
    skip
}

